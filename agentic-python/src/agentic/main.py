"""Entry point for agentic workflow experiments (Claude Agent SDK).

Build your first agent here. See the README for setup (uv) and the ANTHROPIC_API_KEY env var.
"""


def main() -> None:
    print("agentic-python: scaffold ready — build your first agent here.")


if __name__ == "__main__":
    main()
