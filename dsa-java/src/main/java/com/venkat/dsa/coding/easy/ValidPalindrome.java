package com.venkat.dsa.coding.easy;

/**
 * NeetCode / LeetCode — Valid Palindrome
 *
 * <p>Difficulty: EASY
 * <p>Pattern: Two Pointers
 * <p>URL: https://leetcode.com/problems/valid-palindrome/
 *
 * <p>A phrase is a palindrome if, after converting all uppercase letters to lowercase
 * and removing all non-alphanumeric characters, it reads the same forward and backward.
 * Given a string {@code s}, return {@code true} if it is a palindrome, otherwise return {@code false}.
 *
 * <p>Constraints:
 * <ul>
 *   <li>1 &lt;= s.length &lt;= 2 * 10^5</li>
 *   <li>s consists only of printable ASCII characters</li>
 * </ul>
 *
 * <p>Examples:
 * <pre>
 *   isPalindrome("A man, a plan, a canal: Panama") =&gt; true
 *   isPalindrome("race a car")                      =&gt; false
 * </pre>
 *
 * <p>Target: Time O(n), Space O(1)
 *
 * <p>Hint 1: Use two pointers starting at each end; skip non-alphanumeric characters.
 * <p>Hint 2: Compare lowercased characters at both pointers and move them inward.
 */
public class ValidPalindrome {

    public boolean isPalindrome(String s) {
        throw new UnsupportedOperationException("implement me");
    }
}
