/**
 * Hashing structures — reference implementations, one concept per file.
 *
 * <p>Hash map (separate chaining and open addressing), hash set, and an LRU cache (hash map +
 * doubly linked list). Each documents its load factor, collision strategy, and Big-O. See
 * {@code docs/dsa/CONVENTIONS.md}.
 */
package com.venkat.dsa.hashing;
