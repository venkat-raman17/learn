/**
 * Tree and heap structures — reference implementations, one concept per file.
 *
 * <p>Binary tree (and traversals), BST, AVL tree, binary heap, trie, segment tree, and Fenwick
 * tree (BIT). Each documents its ordering/balancing invariant and per-operation Big-O. See
 * {@code docs/dsa/CONVENTIONS.md}.
 */
package com.venkat.dsa.trees;
