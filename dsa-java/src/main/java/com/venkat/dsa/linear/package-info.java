/**
 * Linear data structures — reference implementations, one concept per file.
 *
 * <p>Dynamic array, singly/doubly linked lists, stack, queue, deque, ring buffer. Each class is
 * fully implemented and documented with Big-O; its sibling test verifies it and doubles as a
 * usage example. See {@code docs/dsa/CONVENTIONS.md}.
 */
package com.venkat.dsa.linear;
