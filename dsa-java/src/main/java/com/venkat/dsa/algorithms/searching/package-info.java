/**
 * Searching algorithms — binary search and its variants (lower bound, upper bound, search in a
 * rotated sorted array). Each documents its loop invariant and Big-O. See
 * {@code docs/dsa/CONVENTIONS.md}.
 */
package com.venkat.dsa.algorithms.searching;
